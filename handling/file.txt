form handling and working with the forms
handle backend process of forms and making sure the data coming from ant  frontend lib , framework , templating engines


sessions && cookies ==> hum log kuch bhi data frontend par browser par rakh skte hai and jab bhi aap kuch bhi request backend par karoge wo frontend par saved data automatically backend par chala jayega

